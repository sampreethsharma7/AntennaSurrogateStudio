# Model Versions

Every training run creates a new model version. The active model pointer controls prediction. Older versions remain in the project and can be reactivated unless deleted with confirmation.
