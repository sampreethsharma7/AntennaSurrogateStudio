# Configuring Data

Choose at least one numeric input column and one numeric output column. Inputs are design variables. Outputs are responses the surrogate model predicts. Sweep outputs can use inferred frequency or angle axis metadata.
