# Training XGBoost

Training uses `MultiOutputRegressor` with `XGBRegressor`, a default 20 percent test split, and random seed 42. Advanced settings expose estimators, depth, learning rate, subsampling, child weight, seed, test split, and optional cross-validation folds.
