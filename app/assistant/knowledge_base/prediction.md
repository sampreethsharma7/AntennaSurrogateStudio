# Prediction

The Prediction page creates input fields from the saved schema, warns about values outside training bounds, predicts with the active model, saves prediction history, and exports CSV files.
