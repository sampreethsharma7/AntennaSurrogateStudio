# CSV Formats

Wide CSV format stores design variables and output responses in one table. Split CSV format stores input variables and output responses in separate files with a shared sample ID column.
