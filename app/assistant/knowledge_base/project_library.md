# Project Library

Use New Project to create a self-contained project under `projects/`. Use Open Existing Project to select a project folder containing `project.json`. Built-in examples are synthetic projects that demonstrate the workflow without private data.
