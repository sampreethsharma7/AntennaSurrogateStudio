# Glossary

Input columns are design variables. Output columns are predicted responses. A project is the full folder of data, models, logs, and metadata. A model version is one trained artifact inside a project.
