# Getting Started

Antenna Surrogate Studio is a local desktop app for creating a project, importing simulation or measurement CSV data, choosing input and output columns, training an XGBoost surrogate model, validating it, and making predictions.

Core training and prediction do not require an assistant or internet access.
