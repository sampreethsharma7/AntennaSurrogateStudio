# Troubleshooting

If CSV import fails, check encoding, duplicate column names, required columns, and numeric selected columns. If a model cannot load, preserve the artifact and review package compatibility.
