# Importing Data

The app supports wide CSV files and split inputs/outputs CSV files joined by sample ID. It validates missing values, duplicate sample IDs, nonnumeric columns, constant inputs, duplicate samples, low sample counts, and outliers.
