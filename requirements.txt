# Direct runtime dependencies verified together on the share-ready build.
# Pillow, darkdetect, packaging, narwhals, and threadpoolctl are installed
# transitively by these packages.
customtkinter==6.0.0
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0
joblib==1.5.3
xgboost==3.3.0
